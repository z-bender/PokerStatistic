spring:
  datasource:
    url: jdbc:h2:./db/poker-statistic;IFEXISTS=TRUE
    username: test
    password: test
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate.ddl-auto: update